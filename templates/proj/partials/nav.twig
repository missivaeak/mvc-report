<nav class="nav" onclick="
    this.classList.toggle('visible');
    ">
    <div class="links">
        <a href="{{ path('proj_index') }}">Start</a>
        <a href="{{ path('proj_game') }}">Fortsätt spela</a>
        <a href="{{ path('proj_game_new') }}">Nytt spel</a>
        <a href="{{ path('proj_leaderboard') }}">Topplista</a>
        <a href="{{ path('proj_about') }}">Om</a>
    </div>

    <div class="arrow">
        <
    </div>
</nav>